machine:
  python:
    version: 3.5.1

checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  cache_directories:
    - kanjivg

deployment:
  master:
    branch: master
    commands:
      - ./kanjivg2animation.py
