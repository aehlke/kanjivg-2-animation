machine:
  python:
    version: 3.5.1

checkout:
  post:
    - git submodule sync
    - git submodule update --init

deployment:
  master:
    branch: master
    commands:
      - ./kanjivg2animation.py
