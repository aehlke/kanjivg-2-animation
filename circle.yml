checkout:
  post:
    - git submodule sync
    - git submodule update --init

dependencies:
  cache_directories:
    - kanjivg
    - "~/virtualenvs/venv-3.5.1/"

general:
  artifacts:
    - converted

deployment:
  master:
    branch: master
    commands:
      - python kanjivg2animation.py
